package com.sql1;

public class WhatIsDataBase1 {
    public static void main(String[] args) {
        /**
         A database is an organized collection of data, typically stored electronically, for easy access and management.

         A Database Management System (DBMS) is software that enables users to create, manage, and query databases,
         handling tasks like data storage, retrieval, and security.

         Example - Database: MySQL database "Northwind" storing customer and order data.

         Database Management System (DBMS): MySQL, a software for creating, managing, and querying databases like Northwind.


         ✅ What is SQL?
         SQL (Structured Query Language) is a standard language used for accessing and managing databases.
         It is not a database itself, but a language used to communicate with relational databases. SQL is used to:
         Query data (SELECT)
         Insert data (INSERT)
         Update data (UPDATE)
         Delete data (DELETE)
         Create and modify database structures (CREATE, ALTER, DROP)
         Control access (GRANT, REVOKE)

         SQL was first introduced in the 1970s by IBM and later standardized by
         ANSI (American National Standards Institute) and ISO (International Organization for Standardization).

         ✅ What is MySQL?
         MySQL is an open-source relational database management system (RDBMS) that uses SQL as its query language.
         It was developed by a Swedish company, MySQL AB, and is now maintained by Oracle Corporation.

         MySQL provides:
         A database engine that stores and manages data in tables
         A way to access and manipulate that data using SQL
         Tools and interfaces for managing databases
         It supports various platforms and is commonly used in web applications (like WordPress, Facebook, and many more).

         🆚 Key Differences Between SQL and MySQL
         | **Aspect**               | **SQL**                                                  | **MySQL**                                                   |
         | ------------------------ | -------------------------------------------------------- | ----------------------------------------------------------- |
         | **Definition**           | SQL is a **language** used to manage RDBMSs              | MySQL is a **database system** that uses SQL                |
         | **Type**                 | A **standardized query language**                        | A **specific implementation** of a database engine          |
         | **Functionality**        | Provides commands to operate on databases                | Executes SQL commands to store, retrieve, and modify data   |
         | **Ownership**            | Open standard maintained by ANSI/ISO                     | Owned and maintained by Oracle Corporation                  |
         | **Usage**                | Used in **various RDBMSs** (Oracle, MS SQL Server, etc.) | Specific to the **MySQL RDBMS**                             |
         | **Support for Features** | Depends on the database system                           | MySQL supports **triggers, views, stored procedures**, etc. |
         | **Security**             | SQL itself doesn’t handle security                       | MySQL has features like **user management, privileges**     |
         | **Customization**        | Not applicable                                           | MySQL has configurations, plugins, and tuning options       |

         📌 Example Analogy
         Think of SQL as English — a common language.

         And think of MySQL as a person who speaks English —
         one of many possible people (others being Oracle DB, PostgreSQL, SQL Server, etc.).

         You write SQL queries (language), and MySQL (database system) interprets and executes them.


         */
    }
}
